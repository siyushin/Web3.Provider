{
  "name": "elephant-web3-provider",
  "version": "1.0.0",
  "description": "This README would normally document whatever steps are necessary to get your application up and running.",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf ../dist && parcel build src/index.js -o elaphant.web3.provider.min.js -d ../dist",
    "webpack": "./node_modules/.bin/webpack --config webpack.config.js",
    "watch": "rm -rf ../dist && parcel watch src/index.js -o elaphant.web3.provider.min.js -d ../dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://siyushin1@bitbucket.org/johnny_jiang/elephant-web3-provider.git"
  },
  "keywords": [
    "elephant",
    "wallet",
    "web3",
    "provider"
  ],
  "author": "Si Yushin",
  "license": "ISC",
  "homepage": "https://bitbucket.org/johnny_jiang/elephant-web3-provider#readme",
  "dependencies": {
    "web3-providers-http": "^1.2.11"
  },
  "devDependencies": {
    "webpack": "^4.44.1",
    "webpack-cli": "^3.3.12"
  }
}
